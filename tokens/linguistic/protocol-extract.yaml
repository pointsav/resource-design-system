protocol_name: EXTRACT
operational_rules:
  fidelity_mandate: absolute
  optical_layout_preservation: true
  hallucination_zero_tolerance: true
  prohibit_txt_format: true
flag_definitions:
  "-md": "Output narrative text strictly as Markdown block."
  "-yaml": "Output metadata strictly as valid YAML block."
